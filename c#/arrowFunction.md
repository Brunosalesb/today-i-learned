When you create a method with only one line, like this:

```
public void Activate(){
	Active = true;
};
```

You can use the arrow function to remove the ```{}```, like this ```public void Activate() => Active = true```