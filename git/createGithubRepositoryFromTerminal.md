You can use the command:

```
curl -H "Authorization: token {your-token-here}" https://api.github.com/user/repos -d '{"name":"{my-new-repo-name}"}'

```
to create a new github repository from the command line.
